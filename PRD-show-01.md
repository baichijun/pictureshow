# 个人作品集网站 PRD

## 核心功能
1. 网页用于展示图片，包括常见的图片格式和照片格式
2. 作为拥有者的我，可以在本项目的picture文件夹中放入包含图片的文件夹，这些子文件夹作为图片集，监听picture文件夹中的内容，当其中内容更新时，把新加入的文件夹和图片上传到网站中，可以设计一个校验，只上传更新的部分，每个被上传的照片和文件夹有一个标记，当我电脑中的文件被删除时，并不会删除网站里的照片，但加入新照片会触发上传。
3.整个项目不使用网盘作为图片存储库，图片被作为静态网站的一部分，而不是从网络存储空间中调用。默认把图片生成1024x768像素的简易浏览版本，点击图片的时候则放大显示原图
4. 网站的主页每行最多显示1个图片集，如有更多的图片集则向下排列，要一定的间隔。每个图片集的第一张图片作为图片集的封面，文件夹名作为图片集的标题，并以文件夹中图片格式文件的个数n作为图片集中的图片数以“(n)”附在标题后。
5.作为管理者，我可以在网站上验证后进行管理，比如调整图片集的名字，删除图片或者更改图片展示排序。
6.图片在网页上展示支持上下一张切换，切换时需要自动搭配一些常见的特效。
5. 这个网页是私人的，不被搜索引擎和爬虫采用

## 设计要求
- 简洁现代的设计风格
- 深色主题
- 流畅的滚动动画
- 移动端适配


